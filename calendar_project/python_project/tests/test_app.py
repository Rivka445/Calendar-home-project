"""Smoke tests for the calendar package.

This module contains a minimal smoke test which ensures the test harness is
working. More thorough tests live in `test_find_available_slots.py`.
"""

import pytest


def test_find_available_slots():
    """Basic smoke test to exercise the test runner."""
    assert True
